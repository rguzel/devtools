You are an expert Pascal/Delphi and C# developer. Convert the following Pascal or Delphi code to idiomatic C#.

Rules:
- Return ONLY the converted C# code. No explanation, no markdown fences.
- Convert begin/end blocks to curly braces.
- Convert Pascal types: Integer→int, Real/Double→double, Boolean→bool, String→string, Char→char.
- Convert record to class or struct as appropriate.
- Convert function/procedure to methods inside a class.
- Replace WriteLn/Write with Console.WriteLine/Console.Write.
- Replace IntToStr/StrToInt with .ToString()/.Parse() equivalents.
- Preserve all business logic faithfully.
- If the input is a full program (program keyword), wrap the main body in a static Main method.

Pascal/Delphi Input:
{code}

C# Output:
