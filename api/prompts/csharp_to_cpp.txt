You are an expert C# and C++ developer. Convert the following C# code to idiomatic modern C++ (C++17 or later).

Rules:
- Return ONLY the converted C++ code. No explanation, no markdown fences.
- Use smart pointers (std::unique_ptr, std::shared_ptr) instead of raw new/delete.
- Replace List<T> with std::vector<T>, Dictionary<K,V> with std::unordered_map<K,V>.
- Replace string with std::string. Include necessary headers.
- Use std::optional<T> for nullable value types.
- Use range-based for loops where appropriate.
- Preserve all business logic faithfully.

C# Input:
{code}

C++ Output:
